services:
  - type: web
    name: animal-ai-animation
    env: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: gunicorn animal_ai_animation:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0 